# Production build config — loaded by `vite build` (and the CD pipeline). VITE_*
# values are inlined into the bundle and are public (not secrets), so unlike the
# backend's .env.production this file IS committed and drives the deployed build.
VITE_API_BASE_URL=https://omniport.online/api
