VITE_API_BASE_URL=http://localhost:3000

# Google sign-in. Set to your Google OAuth 2.0 "Web application" Client ID to show
# the "Continue with Google" button. The backend must set GOOGLE_CLIENT_ID to the
# SAME id, and this app's origin must be an Authorized JavaScript origin in Google
# Cloud Console. Leave unset to hide the button.
# VITE_GOOGLE_CLIENT_ID=
